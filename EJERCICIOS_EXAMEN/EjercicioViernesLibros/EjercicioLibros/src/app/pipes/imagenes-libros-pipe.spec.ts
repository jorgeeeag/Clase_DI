import { ImagenesLibrosPipe } from './imagenes-libros-pipe';

describe('ImagenesLibrosPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesLibrosPipe();
    expect(pipe).toBeTruthy();
  });
});
