import { ImagenesVideojuegoPipe } from './imagenes-videojuego-pipe';

describe('ImagenesVideojuegoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesVideojuegoPipe();
    expect(pipe).toBeTruthy();
  });
});
