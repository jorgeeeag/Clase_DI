import { LibrosImagenesPipe } from './libros-imagenes-pipe';

describe('LibrosImagenesPipe', () => {
  it('create an instance', () => {
    const pipe = new LibrosImagenesPipe();
    expect(pipe).toBeTruthy();
  });
});
