import { ImagenesGeneroPipe } from './imagenes-genero-pipe';

describe('ImagenesGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
